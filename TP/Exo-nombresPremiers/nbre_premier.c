#include <stdio.h>
#include <stdlib.h>

int main() {

  int valeur, i, e;

  printf("Bonjour, quelle est le nombre/chiffre?");
  scanf("%d", &valeur);

  printf("Les nombres premiers sont:");

  for (i = 2, i < valeur, i++) {
    int estpremier = 1 for (e = 2, e < i / 2, e++) {
      if (i % e == 0) {
        estpremier = 0;
      }
      if (estpremier = 1) {
        printf("%d", i)
      }
    }

    return 0;
  }
