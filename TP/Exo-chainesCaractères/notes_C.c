/*
 *  "signed int" et "int" sont pareils,
 *  est utilisé pour les nombres / chiffres signés(233, -234),
 *  il utilisé un bit de signe
 *
 */

unsigned int;

/* permet d'économiser ce bit de signe si on veut utiliser des chiffres/nombres
 * sans signe (unsigned
 *
 *    Les tableaux :
 *
 *  char msg[] = "bonjour";
 * assigne le bon nombre de caractère char msg[8] = "bonjour";
 * On donne le nombre de caractère du mot, ne pas oublier \0
 *
 *    int x,
 *    y;
 * int min;
 * min = (x > y) ? y : x;
 * Si x > y alors min = y sinon min = x
 *
 */
